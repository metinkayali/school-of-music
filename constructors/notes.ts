import { note, rest, wn, bn, qn } from './music';
import { Octave, Dur, Pitch, Music } from './music';

export const cff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Cff' });
export const cf = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Cf' });
export const c = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'C' });
export const cs = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Cs' });
export const css = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Css' });
export const dff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Dff' });
export const df = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Df' });
export const d = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'D' });
export const ds = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Ds' });
export const dss = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Dss' });
export const eff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Eff' });
export const ef = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Ef' });
export const e = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'E' });
export const es = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Es' });
export const ess = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Ess' });
export const fff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Fff' });
export const ff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Ff' });
export const f = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'F' });
export const fs = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Fs' });
export const fss = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Fss' });
export const gff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Gff' });
export const gf = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Gf' });
export const g = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'G' });
export const gs = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Gs' });
export const gss = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Gss' });
export const aff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Aff' });
export const af = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Af' });
export const a = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'A' });
export const as = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'As' });
export const ass = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Ass' });
export const bff = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Bff' });
export const bf = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Bf' });
export const b = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'B' });
export const bs = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Bs' });
export const bss = (octave: Octave, dur: Dur): Music<Pitch> =>
  note(dur, { octave, pitchClass: 'Bss' });
